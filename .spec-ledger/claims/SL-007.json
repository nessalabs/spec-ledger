{
  "schemaVersion": 1,
  "id": "SL-007",
  "statement": "GET /v1/context returns VerticalContext with a stable contextDigest for a sealed workstream slice.",
  "severity": "high",
  "required": true
}
