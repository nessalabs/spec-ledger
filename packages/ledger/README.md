# `@nessalabs/spec-ledger`

Claim-adherence ledger and `spec-ledger` CLI.

```bash
npm i -D @nessalabs/spec-ledger@0.1.0-alpha.0
npx spec-ledger init --name my-project
npx spec-ledger verify
```

See the monorepo `DESIGN.md` and `docs/workstreams/consumer-cli-and-npm-publish.md`.
