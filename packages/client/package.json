{
  "name": "@nessalabs/spec-ledger-client",
  "version": "0.1.0-alpha.0",
  "description": "Typed SDK for Spec Ledger — only gateway for UIs and embedders",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@nessalabs/spec-ledger": "0.1.0-alpha.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
