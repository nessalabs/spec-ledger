# Workflow browser checks

The local production build serves /workflows and /workflows/W-012. Following the Workflow link from /workstreams/W-012#process opens the standalone page. The spec page contains only a link, not the editor or process stack. Choose workflow loads the existing editor. The standalone page includes selected steps, required outputs and agent activity. At 390 × 844 the document has no horizontal overflow. Browser errors were empty.

Earlier in this change, a disposable checkout at port 3838 successfully selected Team coding review with two coding steps and skills/team/SKILL.md, previewed and applied it. The real MCP executable observed that selection and began W-001/A-001; the real CLI observed the same attempt. This is protocol execution, not a host agent launcher.

Type checking and the production build pass. Independent regression tests cover malformed imports, origin/token/path/freshness restrictions, retries, required outputs, and rendering history without key warnings.
