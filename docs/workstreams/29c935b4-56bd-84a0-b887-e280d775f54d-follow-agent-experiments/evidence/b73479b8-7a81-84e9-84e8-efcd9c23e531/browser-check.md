# Evidence presentation browser checks

Verified the production build locally against the real workstream.

- Requirement summary opens with keyboard Enter; proof is mounted only after first expansion.
- Captured test names and checkmarks appear in standard output before collapsed test code and command details.
- Compact Run again and Refresh evidence controls remain beside the check description.
- Saved demo PNG loads and enlarges; Escape closes the dialog and restores focus to the enlarge button.
- At 390 px viewport the document width remains 390 px; no framework error overlay or browser errors.
- The earlier demo screenshot is explicitly labeled historical and integrity checked, never used as a verifier pass.
- Independent tests cover honest failed/missing/attested outcomes, unavailable/tampered logs, passive reads, request retry identity and bounded/confined image previews, including truncated PNG/JPEG rejection.

The full runner output is displayed as recorded; no individual test outcomes are inferred from a command exit code.
